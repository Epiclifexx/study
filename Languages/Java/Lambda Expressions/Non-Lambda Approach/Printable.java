package org.example;

// Here, we create an Interface (which is like some sort of contract for the class that implements it)
public interface Printable {

    // Notice this void returning method, it doesn't need to have a body, that's a task for
    // the Class that implements this interface.
    void print();
}
